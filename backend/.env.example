# MongoDB Configuration
MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/your_database

# =============================================================================
# AI MODEL CONFIGURATION - Sistema Multi-Modello con Fallback Automatico
# =============================================================================
# Il sistema supporta più provider AI e li prova automaticamente in sequenza
# fino a trovarne uno funzionante. Configura almeno una API key.
#
# Ordine di priorità modelli:
# 1. Claude Sonnet 4 (Anthropic)
# 2. Claude Sonnet 3.5 (Anthropic)
# 3. GPT-4 Vision (OpenAI)
# 4. GPT-4o (OpenAI)
#
# Se un modello fallisce o è sovraccarico, il sistema passa automaticamente
# al successivo. Questo garantisce alta disponibilità del servizio.

# Anthropic Claude API (Consigliato - Migliore per analisi food)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI API (Opzionale - Fallback alternativo)
# Decomment la riga sotto e inserisci la tua key per attivare i modelli OpenAI
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Origins (separati da virgola)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# JWT Secret Key (usa una stringa lunga e casuale per produzione)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production