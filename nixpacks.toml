[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd backend && npm ci --only=production"]

[phases.build]
cmds = ["echo 'Backend build complete'"]

[start]
cmd = "cd backend && node server.js"
